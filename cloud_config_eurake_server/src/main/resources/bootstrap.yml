spring:
  cloud:
    config:
      name: microservicecloud-config-eureka-client    #名字要跟git上的配置文件名一致
      profile: dev
      label: master
      uri: http://127.0.0.1:3344      #配置服务器访问地址cloud_config_server